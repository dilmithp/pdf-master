apiVersion: v2
name: pdf-core-service
description: Core PDF operations worker (merge, split, compress, rotate) for pdfmaster
type: application
version: 0.1.0
appVersion: "latest"
dependencies:
  - name: pdfmaster-common
    version: "0.1.0"
    repository: "file://../_common"
