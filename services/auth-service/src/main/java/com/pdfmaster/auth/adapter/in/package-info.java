/**
 * Inbound adapters for the auth bounded context (REST controllers, message listeners). Classes in
 * this package translate transport-level input into calls on application services.
 */
package com.pdfmaster.auth.adapter.in;
