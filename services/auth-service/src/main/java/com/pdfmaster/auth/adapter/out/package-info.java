/**
 * Outbound adapters for the auth bounded context (JPA repositories, external clients). Classes here
 * implement ports declared in {@code application}.
 */
package com.pdfmaster.auth.adapter.out;
