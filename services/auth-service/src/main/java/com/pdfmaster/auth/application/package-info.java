/**
 * Application services for the auth bounded context.
 *
 * <p>Hosts use-case orchestrators and outbound port interfaces. Application services depend on
 * {@code domain} types only; they MUST NOT depend on {@code adapter} packages directly.
 */
package com.pdfmaster.auth.application;
