/**
 * Pure domain layer for the auth bounded context.
 *
 * <p>Contains immutable aggregates, value objects, and domain services. Types in this package MUST
 * NOT depend on Spring, JPA, or any adapter package. Persistence mapping lives in {@code
 * adapter.out.persistence}.
 */
package com.pdfmaster.auth.domain;
