/**
 * Configuration layer for the gateway service.
 *
 * <p>Spring {@code @Configuration} classes, {@code @ConfigurationProperties} records, and bean
 * definitions that wire adapters to the application core. Classes here are named with the {@code
 * Config} or {@code Properties} suffix. This is the only place where framework wiring, route
 * declarations, security policy, and rate-limit policy are assembled.
 */
package com.pdfmaster.gateway.config;
